services:
  frontend:
    build:
      args:
        VITE_API_URL: http://localhost:8000

  backend:
    volumes:
      - ./:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    volumes:
      - ./:/app
